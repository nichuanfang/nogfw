# 8度科技频道
from my_global import logging
from my_global import qr_recognize
from my_global import local
import copy
import datetime
import subprocess
from time import sleep
from datetime import datetime


def badukeji_func(channel_id:str,number:int,sleeptime:int):
    """8度科技的频道处理逻辑

    Args:
        channel_id (str): 频道id
        number(int): 爬取次数
        sleeptime(int): 间隔时间

    """  
    logging.info(f'===========================================================================8度科技节点信息获取完毕,共获取有效数据源:个')
    logging.info(f'')
    logging.info(f'')
    logging.info(f'')
    logging.info(f'')
    return []
